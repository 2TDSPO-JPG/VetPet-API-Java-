services:

  api:
    build: .

    container_name: vetpet-api

    ports:
      - "8080:8080"

    environment:
      SPRING_DATASOURCE_URL: jdbc:oracle:thin:@oracle.fiap.com.br:1521:ORCL
      USER_DB_ORACLE: RM565824
      PASSWORD_DB_ORACLE: 120306

    restart: always